{
  "id": "domains",
  "name": "Domains",
  "category": "NFT",
  "description": "域名服务NFT合约，实现去中心化域名服务，支持域名注册、转移和查询",
  "tags": [
    "standard",
    "nft",
    "domain",
    "dns",
    "identity"
  ],
  "language": "go",
  "level": "standard",
  "entryFile": "main.go",
  "helpers": [
    "token"
  ],
  "parameters": [
    {
      "name": "to",
      "type": "address",
      "required": true,
      "description": "接收者地址（MintDomain）"
    },
    {
      "name": "token_id",
      "type": "string",
      "required": true,
      "description": "域名唯一标识"
    },
    {
      "name": "domain_name",
      "type": "string",
      "required": true,
      "description": "域名名称"
    },
    {
      "name": "registrar",
      "type": "string",
      "required": false,
      "description": "注册机构"
    }
  ],
  "risks": [
    "需要实现域名验证（验证域名格式）",
    "需要实现域名续费机制",
    "需要验证所有输入参数"
  ],
  "prerequisites": [
    "完成 Basic NFT 学习模板",
    "了解 NFT 基本概念",
    "了解域名服务应用场景"
  ],
  "examples": [
    "wes contract call <contract_address> --function MintDomain --params '{\"to\":\"<address>\",\"token_id\":\"DOMAIN_001\",\"domain_name\":\"example.wes\",\"registrar\":\"WES Registry\"}'",
    "wes contract call <contract_address> --function TransferDomain --params '{\"to\":\"<address>\",\"token_id\":\"DOMAIN_001\"}'",
    "wes contract call <contract_address> --function QueryDomain --params '{\"token_id\":\"DOMAIN_001\"}'"
  ],
  "version": "1.0.0",
  "author": "WES Contract SDK Team",
  "license": "Apache-2.0",
  "sdkCompatibility": {
    "go": ">=0.1.0-alpha <0.2.0"
  },
  "sinceSdk": {
    "go": "0.1.0-alpha"
  }
}
